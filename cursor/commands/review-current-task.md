Use the **implementation-review-loop** skill in **Current Task Review** mode.

Review only the current guide/phase/task from `.cursor/agents/implementation-state.md`.
Run reviewer -> fixer -> fresh reviewer until `COMPLETE`, `MAX_ROUNDS_REACHED`, or `NEEDS_CLARIFICATION`.
Use `.cursor/agents/implementation-review-state.md` as the blocker ledger; do not paste full reports between agents.
